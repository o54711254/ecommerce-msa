spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false  # Kafka 브로커 없이 컨텍스트 로딩 가능하도록

eureka:
  client:
    enabled: false