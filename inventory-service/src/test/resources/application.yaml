spring:
  jpa:
    hibernate:
      ddl-auto: create-drop  # 테스트 시작 시 테이블 생성, 종료 시 삭제
  kafka:
    bootstrap-servers: localhost:9092
    listener:
      auto-startup: false  # Kafka 브로커 없이 컨텍스트 로딩 가능하도록

eureka:
  client:
    enabled: false
